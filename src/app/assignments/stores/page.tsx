import StoresClient from '@/components/StoresClient';

export default function AssignmentsStoresPage() {
  // Auth + role gate is handled by the parent layout (AssignmentsShell).
  return <StoresClient />;
}
